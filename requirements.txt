# --- core (pipeline runs on these alone) ---
numpy>=1.26
pandas>=2.0
scikit-learn>=1.4
scipy>=1.11
matplotlib>=3.7
joblib>=1.3

# --- optional model engines (auto-detected if installed) ---
# xgboost>=2.0
# lightgbm>=4.0

# --- optional serving (only for src/serving/api.py) ---
# fastapi>=0.110
# uvicorn>=0.29

# --- dev / CI ---
pytest>=8.0
